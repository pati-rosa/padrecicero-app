import React from 'react';
import { Text } from 'react-native';
import { StyledText } from './styles';

export default function RestaurantTitle() {
    return(
        <StyledText>Restaurante Padre Cícero</StyledText>
    );
}